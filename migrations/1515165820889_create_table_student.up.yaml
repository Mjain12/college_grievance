- args:
    sql: CREATE TABLE "student"("fname" text NOT NULL, "mname" text , "lname" text
      NOT NULL, "clg_id" text NOT NULL, "university reg_no" text NOT NULL, "department"
      text NOT NULL, "mobile" text NOT NULL, "email" text NOT NULL, "email_domain"
      text NOT NULL, "password" text NOT NULL, "question" text NOT NULL, "answer"
      text NOT NULL, PRIMARY KEY ("clg_id") )
  type: run_sql
- args:
    name: student
  type: add_existing_table_or_view
